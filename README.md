to run :
 .\mvnw.cmd clean package -Dskiptests
 docker compose up --build

to stop :
    ctrl + C
    docker compose down 
        if you want to delete the setup entirely :
            docker compose down -v

